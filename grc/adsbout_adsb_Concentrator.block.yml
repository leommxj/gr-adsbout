# auto-generated by grc.converter

id: adsbout_adsb_Concentrator
label: adsb_Concentrator
category: '[adsbout]'

parameters:
-   id: gap
    label: gap
    dtype: int
    default: '1'
-   id: ninputs
    label: ninputs
    dtype: int
    default: '1'
    hide: part

inputs:
-   domain: stream
    dtype: byte
    multiplicity: ${ ninputs }

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import adsbout
    make: adsbout.adsb_Concentrator(${gap}, ${ninputs})

file_format: 1
